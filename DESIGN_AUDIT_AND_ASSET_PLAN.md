# 星龙音阶工坊 UI 设计审查与素材计划

Archive note: this is an old prototype planning file. The current product planning source is `docs/README.md`, with `docs/18_CURRENT_PLANNING_REVIEW.md` as the rolling audit source. If this file conflicts with the current `M01-M08 + FG01-FG04 + S01` Star Dino Piano plan, follow the active `docs/` files.

## 设计稿覆盖情况

已覆盖：

- 主玩法：月球工地、吊机零件、发光空位、底部大琴键、当前目标键。
- 成功反馈：零件装上、绿色成功提示、自动进入下一步。
- 关卡地图：8 个任务节点、当前关卡、高亮进度。
- 家长设置：麦克风监听、MIDI 连接、输入状态、重置关卡。
- 版权规避：原创星龙、月球基地、积木零件，不使用奥特曼或已有角色。

未完全覆盖：

- 错误反馈的完整视觉状态，例如恐龙摇头、目标键继续闪、错误音效提示。
- 麦克风/MIDI 的所有状态，例如权限失败、未发现设备、音高识别不稳。
- 每个关卡专属的更精细装配动作，例如车轮滚入、灯泡点亮、屋顶板吸附。
- 角色资产规范，例如恐龙正面/侧面、开心/疑惑/庆祝/鼓励表情。
- 可交付产品级素材，例如启动页、App 图标、音效包、粒子特效、关卡地图背景。

## 当前可运行版本策略

原型阶段先使用 HTML/CSS/SVG 做可动画素材：

- 优点：可快速迭代，能验证玩法、尺寸、触控区域、自动跳关、错误反馈。
- 风险：美术精度不如最终素材，但不会阻碍玩法验证。

正式 iPad App 阶段再生成独立素材包：

- 恐龙角色表：默认、开心、疑惑、庆祝、鼓励、等待、睡眼等表情。
- 恐龙动作：眨眼、点头、跳起、摇头、挥手、坐车、庆祝。
- 积木部件：地板、墙板、屋顶、灯、车轮、桥板、星灯，带高亮/已装/飞行动画状态。
- UI 组件：关卡节点、星星奖励、按钮、家长设置图标、MIDI/麦克风状态图标。
- 特效：正确星星、错误轻震、成功光圈、自动前进倒计时。
- 音效：正确、错误、完成、切关、按钮、积木落位。

## 推荐素材生成顺序

1. 角色模型表：先定星龙伙伴的外形，避免后续所有界面风格漂移。
2. 主玩法部件：吊机、月球基地、5 类琴键状态、8 关零件。
3. 动作表情：只为已验证的核心动作做精细化，避免过早生成无用素材。
4. 关卡地图与家长设置：等主玩法视觉稳定后再细化。
5. 图标和音效：最后统一补齐。

## 当前实现目标

当前 Web 版本应满足：

- 不连 MIDI 也能玩，点击屏幕琴键即可。
- 点错不前进，目标键继续发光。
- 点对会把零件装到月球基地空位。
- 一关完成后自动进入下一关。
- 家长功能折叠，不干扰孩子主玩法。
- iPad 横屏能同时看到工地、目标、琴键。

设计参考图：`ui_design_concept_board.png`
