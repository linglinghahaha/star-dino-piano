# Teaching Proof Matrix

Purpose: turn the current product goal into traceable proof. This is not another audit file. Use it when deciding whether a UI/gameplay change actually improves the core learning product.

`37_TEACHER_AND_CHILD_OBSERVATION_PROTOCOL.md` owns consent/privacy, adult-intervention coding, staged observation execution and pass/return rules. This file owns which skills and levels need proof; its older observation notes cannot override `docs/37`.

The product slice stays narrow until these rows are proven:

- `M01`: first note identity.
- `M03`: touch-only sound matching seed.
- `M08`: five-note climb and F/G preparation.
- `FG03`: E/F/G comparison.
- `S01`: staff bridge from C to G.

## Skill-To-Level Matrix

| Skill | Primary proof level | Child-facing proof | Stable condition | Parent-facing evidence | Current status |
| --- | --- | --- | --- | --- | --- |
| `Do/C` identity | `M01` | child sees `C` on the ordinary UI, hears Xingya call it `Do`, finds the two-black-left white key, and makes a story object land | played once only; no stable claim required | parent says the note has been introduced, not mastered | `341a` preserves child-note-name `160/160`, M01 hierarchy and clean-state proof; real-child observation is still pending |
| `C/D/E` key geography | `M02`, `M07` | child distinguishes left/middle/right around the two-black group | reduced-cue/check pass with at most one wrong input | stable only after a reduced-cue check; M02 guided completion alone remains played | `334g` clean-state and workshop identity pass; M02 remains required in any direct C/D/E course-flow change |
| touch-only sound matching | `M03` | app plays a note and child finds the matching screen key without MIDI/microphone | two complete `Re -> Do` runs, four correct modeled calls total, with no target reveal or strong cue before the successful responses | input route recorded as touch/listen, not microphone mastery; the first complete run remains `played` | `338a` and later baselines pass the single-carrier, hidden-answer, scene-completion and clean-state behavior; set breadth and real-child evidence remain missing |
| C-G five-note ascending route | `M08` | child installs five roof panels with C-D-E-F-G; an eligible second pass checks five seal points instead of rebuilding the roof | full reduced-cue seal check, at most one wrong input, no strong target glow, and no assisted/modeled success or forced check after repeated repair | parent says the five-note route was completed with fewer cues, not that F/G are mastered | `337c` independently proves distinct install/seal story states, world permanence, pacing and natural rest; `343a-p2` preserves all M08 gates |
| `F/G` keyboard locator | `FG01-FG03`, with `M08` exposure and `S01` adaptive support | child finds F and G around the three-black group without solving by color | both F and G have a reduced-cue first-response success and FG03 passes its E-F-G comparison threshold | parent distinguishes M08 exposure, individual F/G home evidence and E-F-G comparison evidence | `343a-p2` retains separated M08 exposure, F/G home and E-F-G comparison evidence plus palette/clean-state gates; real-child observation remains missing |
| `E/F/G` confusion repair | `FG03` | wrong E/F/G neighbor choices reveal the relevant black-key mini-map and route signal | check run, at most one wrong input, future answers hidden before action | repeated wrong pairs and hint strength remain parent evidence | `334g` clean-state/workshop identity pass; separated F/G evidence semantics and real-child observation remain open |
| staff C-G positions | `S01` | dinosaur physically jumps to staff pads from ledger line through second line | staff-check completion, at most two wrong inputs, no strong target-key glow, no F/G remediation trigger | parent says whole `C-G` bridge is stable | `339d` and later baselines pass staff/clean-state proof plus the persistent garden destination; real-child observation remains missing |
| Chapter 3 small-set sound matching | `LS04-LS05` | after a visible/heard reference, child hears an unlabeled neutral piano target and finds it within the already learned C/D or C/D/E key set | LS04 at least 3/4; LS05 at least 4/5 with at least one unassisted correct for each C/D/E; both require no pre-response reveal, no strong/modeled cue, and eligible input evidence | parent says `能在小音组里听后找键`, reports replay/confusion evidence, and never claims absolute pitch | `341a` independently passes LS04 `39/39` and LS05 three consecutive `65/65`, with stable six-viewport evidence; teacher review, device listening and child evidence remain missing |
| Chapter 3 listening contrasts | `LS06-LS07` | child matches a neutral hidden C/G or E/F target after learning the two homes, without using distance, side, flower motion, color, or a prelit boundary as the answer | at least 3/4 first responses for each balanced pair, no pre-response answer, no strong/modeled/visual-assist or experimental-input progression | parent distinguishes `C/G 大距离声音比较` from register mastery and `E/F 相邻声音比较` from keyboard-only boundary help | `342a` independently passes LS06/LS07 `64/64` and both six-viewport phase contracts; teacher/device listening and child evidence remain missing |
| Two-note aural order | `LS08` | after one visible unscored guide pair, child returns four hidden two-note pairs in free time; C-C requires two discrete onsets | one complete check session, first complete response correct for at least 3/4 pairs, no separate-note replay, at most one whole-pair replay, no repaired/cross-session backfill | parent says `能记住两个声音的先后`, reports whole-pair replay and confusion evidence, and does not claim rhythm, low-C, or hand skill | `343a-p2` independently passes `118/118`, truthful parent evidence, unscored low echo and the stable six-viewport/fourteen-state V2 contract; teacher/device listening and child evidence remain missing |
| pre-reading playability | `M01`, `M03`, `M08`, `FG03`, `S01` | child can act from object motion, staff pad, black-key mini-map, sound, and dino gesture | 3-5 real 4-6 children can start, make one wrong input, recover, and finish a short session without adult step-by-step reading | observation notes attached to gate run, including adult interventions | not proven |
| game-teaching pacing and achievement | every formal bundle; current proof focus `LS08` | every 1-2 music actions visibly advance the story, help still permits a satisfying ending, and the planned rest stops autoplay | all 13 checks in `docs/33` pass; any same-session second route has a different story function and is deferred after repeated help/disengagement | parent evidence stays honest while the child keeps permanent world progress | `343a-p2` proves Chapter 3 guide/check separation, neutral roots, help-without-stable, natural rest and unscored story exit in browser; real-child/device evidence remain missing |
| optional microphone input | later listening levels | child can play a simple acoustic note and see a tolerant confidence state | confidence states exist: listening, heard, uncertain, too noisy, confirmed | microphone never counts stable when confidence is missing/low | not release-ready |

## Vertical Slice Exit Gate

Do not call the teaching slice done until all are true:

| Gate | Required evidence |
| --- | --- |
| clean-state parent mastery | local storage reset, run `M01`, `M03`, one reduced-cue route level, and `S01`; parent view separates played-once from stable |
| delayed retention honesty | no current-session check is labeled retained; any retained claim is backed by a later-session/timestamped retrieval without strong target glow |
| reveal-rule correctness | before-action and after-wrong screenshots/DOM for `M03`, `M08`, `FG03`, `S01` |
| color-reduced play | `M07`, `M08`, `FG03`, `S01` work without object color as the answer; A-G color remains a scaffold, never mastery evidence by itself |
| no-reading support | each visible text clue has a non-text equivalent in the same screen state |
| real-child observation | execute `docs/37`: first directional check is one 4-6 child; low-age claim needs 3-5 children, exact build/device/input route, coded adult interventions, start, wrong input, recovery, completion, and natural pause |
| copy/encoding integrity | visible child and parent strings are readable and intentional, with no mojibake or stale fallback copy in the proof screens |
| audio proof | piano tones are in tune, effects are quieter than the piano note, and sound-off mode still leaves visual feedback |
| motion comfort | parent/device reduced motion stops continuous animation and moving particles while physical key depression plus static correct/wrong feedback remain; physical-iPad and real-child comfort proof still required |
| game/teaching integration | `docs/33` phase purpose, meaningful-input budget, world-change frequency, achievement, repair and natural-rest checks for the proof slice; screenshots alone cannot pass this without state/evidence review |
| CSS/bundle safety | no release bundle includes `assets/generated/raw`; no giant CSS override ships without cleanup decision |

Current last independently validated browser baseline: `overhaul-343a-p2`. It retains the full `342a` regression set, independently passes LS08 `118/118`, LS06/LS07 `64/64`, LS05 `65/65`, LS04 `39/39`, child note names `183/183`, sessions `72/72`, clean-state `124/124`, PWA `7/7`, input/audio/accessibility and first-two-chapter gates, quick/strict bundle, generic/legacy coordinate regression, and the stable fixed-directory LS08 six-viewport/fourteen-state V2 contract. It remains partial teaching proof: physical iPad/MIDI/microphone, teacher review, 3-5 child observations and final art/audio are not yet proven.

## Evidence Time Scale

- `played` proves that the child completed a guided story action.
- `stable` proves current-period reduced-cue performance under the family threshold in `09`.
- `retained` requires a different session on a different local calendar date, at least 8 hours after the qualifying stable run, followed by a reduced-cue retrieval that meets the family threshold. The browser event/scheduler implementation exists at `334g`; physical-device timing behavior, teacher review and learning-effect evidence remain unproven.
- The product may continue the story after `played`, but parent copy must not turn `stable` into a long-term learning claim.
- Retention gate evidence must record the earlier session, later review time/session id, cue strength, wrong inputs and result; `334g` implements these browser events, while physical-device timing and learning-effect evidence remain missing.

## First Preschool Observation Series

Do not put the whole product proof slice into one 3-5 minute child session. It changes from note identity to listening to route memory to staff reading too quickly. Use three separate short observations, each with one main teaching axis.

This section schedules the first-course content only. `docs/37` is authoritative for guardian information, privacy, adult scripts, intervention levels, stopping, samples and release retest.

Canonical course bundles live in `03_CONTENT_ROADMAP.md`; the parent-readable explanation lives in `24_HUMAN_STORY_AND_LESSON_BOOK.md`.

Implementation status: `S01-mini` exists as observation tooling and does not write full S01 mastery. The first/second-chapter session scheduler, natural-rest resume and separate later-session retained event exist in the `334g` browser runtime and pass `72/72`; Chapter 3-5 scheduling, physical-iPad behavior and child-usability proof remain missing.

Observation standard:

- one child is a directional usability signal only;
- do not claim low-age readiness until 3-5 children have completed the relevant observation sessions after the current visual/audio pass;
- every adult hint must be logged, especially pointing, reading, or explaining the target;
- rerun the observation after final art/audio/CSS changes because children may follow different visual anchors.

1. **Observation A, keyboard home (3-5 minutes):** `M01`, then the C-D-E lamp actions in `M02`. Stop when the three lamps settle.
2. **Observation B, listening (about 3 minutes):** one short known-key warm-up, then the two modeled calls in `M03`. Stop when the wheel reaches its bay.
3. **Observation C, staff bridge (3-5 minutes):** a brief C-D-E keyboard reminder, then `S01-mini`. Jump two or three staff pads and stop at the visible rest star.

`M08` and `FG03` remain required proof levels. The guided M08 construction route may be observed in canonical course order before the dedicated F/G lessons, but it proves only whether the child understands the five-step construction and first full route. Its reduced-cue seal check, color-reduced use and any F/G locator claim are observed only after `FG01-FG03`; one guided M08 pass never proves F/G stable.

This observation series is not the same as full S01 mastery. Observation C checks whether a young child can understand the staff-bridge mechanic in a short session. Full S01 mastery still requires the complete C-G bridge and reduced-cue/staff-check conditions above. The mini route does not write stable S01 mastery or auto-enter `staff-check`.

Pass condition:

- child knows where to look after the screen loads;
- child can press the screen keyboard without adult pointing at every note;
- one wrong input produces a recoverable, non-scary teaching moment;
- the child sees a clear story result;
- the session ends without needing a manual close/next-button explanation.
- the observer can describe which cue actually guided the child: sound, dino motion, staff pad, black-key mini-map, key geometry, or text.

## F/G Remediation Rules

Use this table when S01 exposes F/G weakness:

| S01 issue | Next action | Re-entry rule |
| --- | --- | --- |
| misses `Fa/F` once | show stronger three-black-left mini-map on the next F pad | continue S01, do not mark stable yet |
| misses `Fa/F` twice | route next replay to `FG01` or `FG03` depending on whether E/F confusion appeared | return to S01 only after a reduced-cue F success |
| misses `Sol/G` once | show three-black-left-middle mini-map and compare F/G positions | continue S01, do not mark stable yet |
| misses `Sol/G` twice | route next replay to `FG02` or `FG03` depending on whether F/G confusion appeared | return to S01 only after a reduced-cue G success |
| misses both F and G | route to `FG03` comparison before S01 replay | S01 starts with stronger first-run F/G hints |

## Next Implementation Targets

1. Keep `overhaul-347a-c4-r01a` / `af9aa28` as the current browser teaching baseline. It preserves Chapters 1-3 and LP01-LP03, retains the full teaching-audio lifecycle, and adds only the accepted isolated C4-R01A opening-review scheduler core.
2. Implement only `LP04` under `docs/51` next, and in the same candidate complete `C4-R01B` under `docs/48`: real C4-03 actions, at most one opening review, same-session continuation after success, and a safe stop before any LP04 presentation after difficulty.
3. Preserve all Chapter 1-3, LP01-LP03 and R01A evidence. LP04/R01B cannot rewrite high/low C, low-C-home or C-D-E route results, clear earlier needs-practice, or turn guided/assisted/modeled progress into stable.
4. Re-prove against real LP04 actions that any opening review is followed by a story-first review-free formal session, equal-priority candidates cannot starve, resume sessions never insert review, and the frozen A-stage scheduler behavior remains unchanged.
5. Keep LP05+, Dongdong's full reveal, bass staff, left-hand tasks and broader `docs/34` implementation locked until LP04 and C4-R01B pass together. The A-stage fixtures cannot substitute for the real C4-03 end-to-end evidence.
6. Only after `docs/34` passes, implement `docs/35`: core relay first, resumable TH05 stages, then optional paired/together routes with identical permanent rewards.
7. Keep `16_ASSET_MANIFEST.md` as the asset/audio release ledger and keep all current media candidates out of runtime until the latest coordinate, source and audio-priority gates pass.
8. Measure a frozen candidate on a real iPad speaker/headphones and verify Safari reduced motion, background resume, touch timing and MIDI before treating browser checks as device proof.
9. Run the staged preschool observations under `docs/37` with 3-5 children after the relevant visual/audio state is stable; record intervention codes, wrong/recovery behavior and whether the child noticed the story consequence, then repeat after final art/audio/native changes.
10. Keep copy integrity and strict bundle checks passing, and continue semantic CSS consolidation only between behavior milestones, not inside an active teaching slice.
